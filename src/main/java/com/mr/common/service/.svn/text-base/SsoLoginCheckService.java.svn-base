package com.mr.common.service;

import javax.servlet.http.HttpServletRequest;

import org.springframework.stereotype.Service;

@Service
public interface SsoLoginCheckService {

    /**
     *  현대오일뱅크 ssoCheck서비스
     *  sso 로그인을 성공하였을 경우 id를 반환하고 실패하였을 경우 null을 반환한다.
     *
     * @param request
     * @return 로그인 사용자 id
     */
    String getHdoSsoLoginUser(HttpServletRequest request);

}
